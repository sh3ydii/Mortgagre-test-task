import { JwtService } from '@nestjs/jwt';
import { AdminJwtService } from './services/admin-jwt.service';
import { UsersService } from '../user/users.service';
import { type User } from '../user/schemas/users';
import { CreateTokenDto } from './dto/create-token.dto';
import { RefreshTokenDto } from './dto/refresh-token.dto';
import { AdminLoginDto } from './dto/admin-login.dto';
import { AdminRefreshTokenDto } from './dto/admin-refresh-token.dto';
import { JwtPayload } from './interfaces/jwt-payload.interface';
import { AdminJwtPayload } from './interfaces/admin-jwt-payload.interface';
import { TokenResponse } from './interfaces/token-response.interface';
import { AdminTokenResponse } from './interfaces/admin-token-response.interface';
import { Request } from 'express';
import { AccountService } from '../account/account.service';
import { AccountTokenService } from '../account-token/account-token.service';
import { RefreshTokenService } from '../refresh-token/refresh-token.service';
import { type Account } from '../account/schemas/accounts';
export declare class AuthService {
    private readonly db;
    private readonly usersService;
    private readonly jwtService;
    private readonly adminJwtService;
    private readonly accountService;
    private readonly accountTokenService;
    private readonly refreshTokenService;
    private readonly logger;
    private readonly botToken;
    private readonly refreshTokenExpiresIn;
    constructor(db: any, usersService: UsersService, jwtService: JwtService, adminJwtService: AdminJwtService, accountService: AccountService, accountTokenService: AccountTokenService, refreshTokenService: RefreshTokenService);
    private validateTelegramInitData;
    private extractUserIdFromInitData;
    createToken(dto: CreateTokenDto, req?: Request): Promise<TokenResponse>;
    refreshToken(dto: RefreshTokenDto, req?: Request): Promise<TokenResponse>;
    removeAllUserTokens(userId: string): Promise<void>;
    private generateTokens;
    validateJwtPayload(payload: JwtPayload): Promise<User | null>;
    adminLogin(dto: AdminLoginDto, req?: Request): Promise<AdminTokenResponse>;
    adminRefreshToken(dto: AdminRefreshTokenDto, req?: Request): Promise<AdminTokenResponse>;
    private generateAdminTokens;
    validateAdminJwtPayload(payload: AdminJwtPayload): Promise<Account | null>;
}
